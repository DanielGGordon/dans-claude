# Runtime / cache — never commit these
backups/
cache/
debug/
downloads/
file-history/
history.jsonl
paste-cache/
plans/
projects/
shell-snapshots/
telemetry/
policy-limits.json
plugins/

# Secrets — double-check before committing settings.json
# (it may contain API keys depending on your setup)
