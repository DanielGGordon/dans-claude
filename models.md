# Moved (2026-07-21)

This file was split in two:

- **`~/.claude/model-selection.md`** — WHICH model to use and WHEN: rankings,
  task-type guidance, subagent/workflow model assignment. Read this before any
  delegation (Agent tool or Workflow tool).
- **`~/.claude/model-usage.md`** — HOW to invoke a model once chosen: canonical
  CLI wrappers (Codex, Cursor), native Claude routing, auth/error rules.

Update any reference pointing here to the appropriate new file.
